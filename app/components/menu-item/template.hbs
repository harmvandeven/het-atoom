{{#if this.route}}
  {{#if @model.hash}}
    <LinkTo @route={{this.route}} @model={{@model.hash}}>
      <li class='link'>
        <div class='id'>
          {{@model.id}}
        </div>
        <div class='title'>
          {{@model.title}}
        </div>
      </li>
    </LinkTo>
  {{else}}
    <LinkTo @route={{this.route}}>
      <li class='link'>
        <div class='id'>
          {{@model.id}}
        </div>
        <div class='title'>
          {{@model.title}}
        </div>
      </li>
    </LinkTo>
  {{/if}}
{{else}}
  <li>
    <div class='id'>
      {{@model.id}}
    </div>
    <div class='title'>
      {{@model.title}}
    </div>
  </li>
{{/if}}

{{yield}}